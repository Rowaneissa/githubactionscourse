name: First Workflow


on:
  push:
    branches: 
       - main


jobs:
  example-job:
    runs-on: ubuntu-latest



    steps:
      - name: print a welcome message
        run: echo "welcome"

      - name: Checkout
        uses: actions/checkout@v7.0.1
        
      - name: list repo files
        run: ls -la
        
